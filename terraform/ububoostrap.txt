#cloud-config
package_upgrade: false
packages:
  - docker.io
  - docker-compose

runcmd:
  - touch /home/labuser/.hushlogin
  - [ bash, -lc, "systemctl enable --now docker" ]
  - [ bash, -lc, "docker rm -f juice-shop >/dev/null 2>&1 || true" ]
  - [ bash, -lc, "docker run -d --name juice-shop --restart unless-stopped -p 3000:3000 bkimminich/juice-shop:latest" ]


